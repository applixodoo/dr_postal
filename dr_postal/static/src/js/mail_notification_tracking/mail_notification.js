/** @odoo-module **/

/**
 * Mail notification postal tracking.
 * 
 * The postal_state is added to notification data via the Python _to_store method.
 * No additional JS model patching needed - the data flows through automatically.
 */
